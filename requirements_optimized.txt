# Dependências otimizadas para VPS com recursos limitados
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1

# Whisper (versão otimizada)
openai-whisper==20231117
# Torch CPU wheels (sem "+cpu" no PyPI)
torch==2.2.2
torchaudio==2.2.2

# Fix compatibilidade de binários com PyTorch/Whisper
numpy==1.26.4

# OCR (opcional - apenas Tesseract)
pytesseract==0.3.10
Pillow==10.1.0

# Monitoramento de sistema
psutil==5.9.6

# (ffmpeg é instalado via apt no Dockerfile)

